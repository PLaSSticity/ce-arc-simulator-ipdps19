# SB: Create a local copy of this file with appropriate values.

# Use a list to pass multiple values if needed (e.g., emails).
# Duplicate keys are not allowed in a section.

[USER]
USER = ["your machine user name"]

[EMAIL]
EMAIL = ["your email"]

[SYNC]
SOURCE = ["your machine name"]
RESULT = ["your machine name"]
